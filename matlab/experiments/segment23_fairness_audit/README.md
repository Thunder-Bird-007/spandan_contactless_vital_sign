# Segment 23 — fairness / native-form audit

Every alternative pulse-extraction, ROI, filtering and HR-read-out method tried in Segments 6, 7, 9, 18, 21 and 22 lost to the project's CHROM/POS defaults. This segment re-runs each "EVALUATED, NOT ADOPTED" candidate in its native or completed form (as its own paper specifies, or with its missing stage built) and, in parallel, tests whether CHROM/POS's lead survives de-tuning. Background and the candidate list: `matlab/docs/Literature_Review_Master.md` §1–3 and §7. Start with `MASTER_REPORT.md`; per-task detail in `task1_…` to `task7_…/REPORT.md`. Production code in `matlab/src/` was not modified (`common/protected_files_verification.txt`); each task keeps its own modified copies in its `src/`, shared helpers (pool, metrics, chain) are in `common/`.
