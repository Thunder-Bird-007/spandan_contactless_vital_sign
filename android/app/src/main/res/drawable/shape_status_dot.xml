<?xml version="1.0" encoding="utf-8"?>
<!-- Segment 16 Task 3: the small colored dot inside each status pill.
     Tinted at runtime (setBackgroundTintList) to match the pill's own
     status color, same one-shape-many-tints approach as bg_status_pill.xml. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="oval">
    <solid android:color="@color/status_ok" />
</shape>
